{
    "name": "smart-defi-router-ai",
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "build": "tsc -p .",
        "test": "vitest run",
        "test:watch": "vitest",
        "plan": "ts-node ai/keeper/cli.ts plan --mode intra --current curve --amount 25000 --out out/plan.json",
        "execute": "ts-node ai/keeper/cli.ts execute --plan out/plan.json"
    },

    "devDependencies": {
        "typescript": "^5.6.3",
        "ts-node": "^10.9.2",
        "vitest": "^2.1.3"
    },
    
        "dependencies": {
        "ethers": "^6.13.2"
    }
}